# pydeck_install
  ```
    pip install jupyter
    pip install pydeck
    # 원도우 설치시 cmd 관리자 모드 실행 
    jupyter nbextension install --sys-prefix --symlink --overwrite --py pydeck
    jupyter nbextension enable --sys-prefix --py pydeck
  ```
